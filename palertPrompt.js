//method to display a modal dialog box asking the user for input. 
//This is often used to gather information from the user within a web browser before proceeding with an action

// prompt: Purpose: Get user input.
// Use cases:
// When you need data directly from the user, like their name, age, or preferences.
// For interactive demos or quizzes.
// To collect user feedback.

// console.log:Purpose: Print messages to the browser's console for debugging or logging purposes.
// Use cases:
// Inspecting variable values at different points in your code.
// Debugging logic errors.
// Tracking program execution flow.
// Keeping a log of events in your application.

let p = prompt("message")
console.log(p);